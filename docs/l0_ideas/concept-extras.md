# Concept Extras (§7-10)

[concept.md](./concept.md) §1-6 の続編。

## 8. マイルストーン / 実装ステップ

### M1：ランチャー単体

**目的：** 「毎日使えるもの」を最速で作る。起動体験のコアを確立する。

**成功条件：**

- ゲーム起動・Blender複数バージョン切り替え・Claude Code起動の3ユースケースをノーエラーで実行できる
- ホットキー → 名前入力 → Enter → 起動のフローを10回繰り返してエラーが発生しない
- 常駐時メモリ 100MB以下、ホットキー起動から表示まで P95 2秒以内を計測で確認できる

**注記：** M1はプラグイン構造なしで実装する。ただしプラグインインターフェースの境界定義（どこで切り分けるか）はM1設計時に確定させる。

---

### M2a：コマンドパレット拡張

**依存：** M1完成・安定後
**目的：** パワーユーザー向けの操作を強化し、単なるランチャーから脱却する。

**成功条件：**

- CLIからアイテム起動できる
- クリップボード履歴・スニペット・電卓が内蔵コマンドとして呼び出せる
- カテゴリプレフィックスで絞り込み起動できる

---

### M2b：ワークスペース

**依存：** M2a完成後
**目的：** 「ホーム画面」を作り、ツールとプロジェクトをビジュアルに整理できるようにする。

**成功条件：**

- 2つ以上のページを作成し、内蔵ウィジェットを配置して日常運用できる
- 監視フォルダが新規ファイルを検知してアイテムを自動登録できる

---

### M2c：Agent-first CLI

> PH-003-H で MCP サーバーを除去し、Agent-first CLI に移行済み。

**依存：** M2b完成後（並行着手可）
**目的：** Claude CodeからArcagateをCLI経由で操作できるようにする。

**成功条件：**

- Claude Codeから `arcagate_cli run <name>` でアイテムを起動できる
- Claude Codeから `arcagate_cli search <query>` でアイテムを検索できる

---

### M3：コンテキストメニュー統合 ⚠️ 凍結

> **凍結**: PH-004 で凍結。優先度見直し待ち。

**依存：** M2a完成・安定後
**目的：** ~~ファイル操作をArcagateの登録コマンドと直接繋ぐ。~~

~~**成功条件：**~~

- ~~SendTo相当の統合でファイルをスクリプトに渡せる（第1段階）~~
- ~~右クリックメニューに登録コマンドが表示され、複数ファイル選択して渡せる（第2段階）~~

---

### M4：ファイルマネージャー・AI連携深化 ⚠️ 凍結

> **凍結**: PH-004 で凍結。優先度見直し待ち。

**依存：** M2bおよびM3完成・安定後
**目的：** ~~ファイル操作を統合し、AIとの連携を深める最終形態。~~

~~**成功条件：**~~

- ~~タブ付きファイルブラウザが日常のエクスプローラー代替として使える~~
- ~~10万件規模のファイル検索が1秒以内で応答する~~
- ~~Claude CodeからCLI経由でファイル操作（タグ付け・フォルダ作成）ができる~~

---

## 9. 継続判断基準

### 継続する条件

- M1完成後、2週間以上毎日自分が使い続けている
- ホットキー起動からUI表示のP95が常時2秒以内に収まっている
- 常駐メモリが100MB以下を維持している

### 再検討する条件

- M1完成後2週間で自発的に使わなくなった場合 → コアUXの再定義を検討
- Tauriのアップデートや依存ライブラリの破壊的変更で維持コストが急増した場合 → スタック見直しを検討
- M2のプラグイン移行でコア全体の大規模リファクタが必要になった場合 → M1のインターフェース設計に立ち返る

---

## 10. 技術スタック概要

| レイヤー                   | 選択   | 理由                                           |
| -------------------------- | ------ | ---------------------------------------------- |
| UIフレームワーク           | Svelte | 軽量・高速・バンドルサイズ小                   |
| バックエンド               | Rust   | ファイル操作・検索・ログ処理の速度確保         |
| デスクトップフレームワーク | Tauri  | Electronより軽量、Rustと自然に統合             |
| ローカルDB                 | SQLite | ローカル完結・ゼロコスト・全マイルストーン共通 |

**SQLiteスキーマについて：** タグ・ファイルパス・ワークスペース・プラグイン情報のテーブル境界設計（どのテーブルが必要か、リレーション構造）はM1時点で行う。ただしテーブルの作成自体は各マイルストーンでインクリメンタルに行う（rusqlite_migrationによりマイグレーションコストは低い）。

---

## 11. 競合と参考

| ツール                                         | 特徴                                         | 参考にできる点                       | 差別化                                                              |
| ---------------------------------------------- | -------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------- |
| [Raycast](https://www.raycast.com/)            | モダンUI・プラグインエコシステム豊富         | プラグイン思想・クイックアクセスのUX | Windowsβ継続中・バイナリサイズ大・ワークスペース/ファイル管理が弱い |
| [Flow Launcher](https://www.flowlauncher.com/) | OSS・プラグイン豊富・軽量                    | プレフィックス操作・プラグイン構造   | UIが地味・ワークスペース概念なし                                    |
| [Listary](https://www.listary.com/)            | ランチャー＋ファイル検索・軽量・Explorer統合 | ファイル検索統合・軽量設計           | ウィジェット/ワークスペース概念なし・有償                           |
| [Playnite](https://playnite.link/)             | ゲームライブラリ統合・OSS                    | プラグイン構造・ゲーム管理UI         | ゲーム特化・開発ツール管理なし                                      |

**差別化の核：** 「ゲーム・開発ツール・スクリプト・URLを同一アイテムとして扱い、ワークスペースとCLIで拡張できる」組み合わせを持つ競合がない。

---

## 12. 懸念事項

- **~~M3：コマンドライン長制限~~**（⚠️ M3 凍結により保留）
  ~~複数ファイル選択時はOS→プロセス引数経由になるため、パス長制限やクォート処理問題が発生しやすい。大量選択時は一時ファイル（paths.txt）経由の逃げ道をM3設計時に組み込む。~~

- **~~M2：MCP権限境界~~**（✅ 解消済み — PH-003-H で MCP 除去。Agent-first CLI に移行し、この懸念は消滅）

- **M2：プラグイン移行リファクタのリスク**（M1設計時に対処）
  M1をプラグイン構造なしで作った場合、M2移行時に大規模リファクタが発生するリスクがある。インターフェース境界（コアとプラグインの切り分け）だけはM1設計時に確定させることで軽減する。

- **~~M4：ファイルインデックスとパス監視の二重実装~~**（⚠️ M4 凍結により保留）
  ~~パス追跡（M2）とファイルインデックス（M4）で同種の処理が必要になる。共通のファイル監視モジュールを最初から設計しておき、二重実装を防ぐ。~~

- **Tauriバージョン依存**
  Tauriのアップデートに追従できないと全体が止まる。Tauri本体のAPIに直接依存する部分を最小化し、変更に強い構造を維持する。

- **Steam API等ゲームライブラリ自動同期（未決）**
  手動登録が基本方針だが、Steamライブラリの自動同期（インストール済みゲームの自動取り込み）をするかどうかは判断保留。対応する場合はM2以降でプラグインとして実装する方針。

---

## 13. 設計思想

### 全機能はプラグイン——コアは「動かす仕組み」だけを持つ

コアが直接持つのは「アイテムの登録・起動・常駐」のみ。コマンドパレット・ワークスペース・CLIはすべてプラグインとして実装する。公式プラグインとサードパーティプラグインは同一のインターフェースで動作する。この原則により、1つのプラグインのバグがコアに波及しない。

### サービス層の一元化——UI・CLIは同じ入口を使う

UI・CLIはサービス層という共通インターフェースを経由する。これにより、UI操作とCLI操作が同一結果を保証でき、テストも可能になる。

### 初期設計に組み込む共通基盤

M1時点で以下の3点をスキーマ・インターフェース設計に折り込む。後から追加するコストが特に高い領域のため：

- **SQLiteスキーマの境界設計**：タグ・ファイルパス・ワークスペース・プラグイン情報のテーブル構造を設計（作成は各マイルストーンでインクリメンタルに実施）
- **ファイル監視モジュールの抽象化**：パス追跡（M2）で使用する共通境界の定義（~~M4ファイルインデックスは凍結~~）
- **プラグインインターフェースの境界定義**：M1は使わないが、M2移行時の全面リファクタを防ぐために定義だけ確定させる
