# L1 Requirements

L1 = **要件** (REQ-ID 付き / テスト可能仕様)。 現在は **gen-l1 未着手** で空の状態。

将来、 L0 (`../l0_ideas/motivation.md`) を元に gen-l1 (要件抽出 task) で `vision.md` 等の REQ-NNNN 付き要件 doc を生成する。 生成完了までこの dir は **intentionally empty**。

## 想定 layout

```
l1_requirements/
└── vision.md    ← gen-l1 で生成、 機能要求 REQ-NNNN 一覧 + 非機能要求 + 制約
```

(generated 後にこの README.md は削除予定)
