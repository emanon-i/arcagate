# PH-20260425-251 フロント/バックエンド処理分類表 + IPC 境界調査

- **フェーズ**: batch-59 Plan C（整理系 3）
- **status**: done
- **開始日**: -

## 目的

各機能の「実行場所・処理重さ」を分類し、Rust 移譲候補と IPC 境界の全体像を
`docs/l2_architecture/frontend-backend-split.md` に文書化する。

## 技術方針

### 処理分類表（対象機能）

| 機能                | 実行場所   | 処理重さ | 移譲候補 |
| ------------------- | ---------- | -------- | -------- |
| アイテム起動        | Rust       | 中       | 現状維持 |
| ファイル watch      | Rust       | 高       | 現状維持 |
| テーマ CSS 変数適用 | フロント   | 低       | 不要     |
| 検索フィルタ        | Rust (SQL) | 中       | 現状維持 |
| タグ統計集計        | Rust (SQL) | 中       | 現状維持 |
| ドラッグ＆ドロップ  | フロント   | 低       | 不要     |
| アイコン抽出        | Rust       | 高       | 現状維持 |

### IPC コマンド全列挙

`src-tauri/src/commands/` の全 `cmd_*` 関数をリスト化し、
フロントの `invoke` 呼び出しとの対応・型定義同期状態を確認する。

## 受け入れ条件

- [ ] `docs/l2_architecture/frontend-backend-split.md` 存在
- [ ] 全 `cmd_*` コマンドと対応 IPC の対応表
- [ ] Rust 移譲候補とそのトレードオフ
- [ ] `pnpm verify` 全通過（コード変更なし）
