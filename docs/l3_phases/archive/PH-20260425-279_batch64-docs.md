---
id: PH-20260425-279
status: todo
batch: 64
type: 整理
---

# PH-279: batch-64 整理（dispatch-log + archive + docs 棚卸し）

## 背景・目的

batch-64 完了エントリの記録と L3 plan archive。
また、docs 棚卸しとして不要・重複ドキュメントを削減し、CLAUDE.md の `@` 自動ロード 3 本に集約する。

## 作業内容

### dispatch-log 更新

- batch-64 計画エントリ（PH-275〜279）
- batch-64 完了エントリ（実施後に追記）

### L3 Plan archive

batch-63 完了分（PH-270〜274）を `docs/l3_phases/archive/` に移動。

### docs 棚卸し（不要・重複削減）

確認対象:

- `docs/desktop_ui_ux_agent_rules.md`（内容が ux_standards.md と重複している箇所があれば統合）
- 廃止済み Plan や古い batch ドキュメントの archive 状況確認
- `docs/l2_architecture/` の新規ファイル（codex-review / bundle-baseline 等）が正しく参照可能か確認

## 受け入れ条件

- [ ] dispatch-log に batch-64 完了エントリが記録される
- [ ] PH-270〜274 が archive に移動する
- [ ] `pnpm verify` 全通過（docs のみ変更）
