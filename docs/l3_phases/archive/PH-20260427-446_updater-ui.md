---
id: PH-20260427-446
status: done
batch: 100
type: 改善
era: Distribution Era
---

# PH-446: Updater UI (Settings + 自動チェック)

## 問題

batch-97 PH-442 で tauri-plugin-updater 統合 (基盤) → UI 実装が次。

## 改修

`src/lib/components/settings/UpdaterSettings.svelte`:

- 「アップデート確認」ボタン → `check()` IPC
- 利用可能なら version + 内容 + [今すぐインストール] ボタン
- インストール → download + restart confirm
- Settings > General に統合

`src/lib/state/updater.svelte.ts` 新設:

- 自動チェック (起動時 + 24h 毎)
- 利用可能時 toast + アイコンバッジ

## 受け入れ条件

- [ ] UpdaterSettings.svelte 新設
- [ ] Settings > General に統合
- [ ] 起動時自動チェック + 24h 間隔
- [ ] 利用可能 toast + Settings バッジ
- [ ] vitest 1 ケース (チェック頻度ロジック)
- [ ] `pnpm verify` 全通過
