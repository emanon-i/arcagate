- [PH-CF-100] **high**: Workspace deletion still risks orphaned widget references unless cleanup of both `item_id` and `item_ids` is enforced transactionally with workspace/item/tag deletes. Add a Rust integration test around [`workspace_service.rs`](E:/Cella/Projects/arcagate/src-tauri/src/services/workspace_service.rs) + [`workspace_repository.rs`](E:/Cella/Projects/arcagate/src-tauri/src/repositories/workspace_repository.rs) that seeds mixed widget payloads and asserts zero dangling references post-delete.
- [PH-CF-100] **med**: The delete flow is not recurrence-safe if `delete_items` intent is implicit. Make `delete_items: boolean` mandatory in [`workspace_commands.rs`](E:/Cella/Projects/arcagate/src-tauri/src/commands/workspace_commands.rs) and [`workspace.ts`](E:/Cella/Projects/arcagate/src/lib/ipc/workspace.ts), reject missing field, and add command-level contract tests for both branches.
- [PH-CF-200] **high**: OS file-drop path currently bypasses grid placement logic, so a local UI fix may not solve real E1 drops. Add Playwright/Tauri E2E that injects `tauri://drag-drop` with pointer coordinates and verifies created widget position matches grid conversion in [`WorkspaceWidgetGrid.svelte`](E:/Cella/Projects/arcagate/src/lib/components/workspace/WorkspaceWidgetGrid.svelte).
- [PH-CF-200] **med**: Async drop handling can place widgets on the wrong page if tab/page changes mid-operation. Capture `pageId` at drop start in [`+page.svelte`](E:/Cella/Projects/arcagate/src/routes/+page.svelte) and add a regression test that switches page during drop but still inserts on the original target.
- [PH-CF-400] **high**: Scanner redesign can break existing hide/override behavior because UI keys by `folderPath`. Add an explicit stable-identity contract (normalized absolute folder path + deterministic ordering) and back-compat tests in [`ExeFolderWatchWidget.svelte`](E:/Cella/Projects/arcagate/src/lib/widgets/exe-folder/ExeFolderWatchWidget.svelte) + scanner tests.
- [PH-CF-400] **med**: Depth-limit checks alone are too weak; recurrence risk remains for symlink loops, permission errors, and slow cancel. Extend [`exe_scanner_service.rs`](E:/Cella/Projects/arcagate/src-tauri/src/services/exe_scanner_service.rs) tests to cover loop safety, dedupe, permission-denied continuation, and cancel-latency budget.
- [README] **high**: Phase ordering under-specifies shared launcher-path dependency, so later PHs can reintroduce inconsistent execution behavior. Add a hard dependency note that opener-cascade contract lands before widget fixes, plus a CI grep audit banning direct launch calls outside the approved adapter.
- [README] **med**: Destructive-confirm recurrence prevention is local, but `window.confirm` remains in multiple screens. Add a repo-wide static check (e.g., `scripts/audit-no-native-confirm.sh`) that fails on `window.confirm|alert|prompt` in `src/` except explicit allowlist.
- [README] **med**: Widget default drift (component vs settings vs `defaultConfig`) is already present, so PH-level fixes can regress silently. Add a machine check that compares defaults across [`src/lib/widgets/*/index.ts`](E:/Cella/Projects/arcagate/src/lib/widgets) and corresponding `*Settings.svelte` fallbacks.
- [README] **high**: Library “hidden item” fix at repository layer can unintentionally affect palette/favorites/search consumers. Add call-site matrix tests for [`item_repository.rs`](E:/Cella/Projects/arcagate/src-tauri/src/repositories/item_repository.rs) and split API semantics (`include_disabled` flag or separate query) with explicit acceptance per surface.
- [README] **med**: Overlay/drag-region fixes are likely to be patched per-screen and regress elsewhere. Add a cross-overlay E2E suite covering settings/help/onboarding/setup overlays to verify title drag-region behavior remains valid in each state.
- [PH-CF-1000] **low**: Performance acceptance is likely qualitative unless hard budgets are encoded. Extend [`tests/perf/widget-perf.spec.ts`](E:/Cella/Projects/arcagate/tests/perf/widget-perf.spec.ts) and startup perf checks with explicit thresholds for scanner-heavy workspaces and fail CI on regression.

**TOP 3 IMPROVEMENTS**

1. Add **transactional orphan-reference deletion contract tests** for workspace delete (covers the highest data-integrity risk).
2. Add **true OS drag-drop E2E placement tests** (not just HTML5 drop) with zoom/scroll/page-switch scenarios.
3. Add **repo-wide machine audits** for recurrence prevention: no native confirms, no direct launch bypasses, and widget-default parity checks.
