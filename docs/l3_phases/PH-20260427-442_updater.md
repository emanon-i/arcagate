---
id: PH-20260427-442
status: todo
batch: 97
type: 改善
era: Distribution Era
---

# PH-442: tauri-plugin-updater 統合 (Codex Q4 #2)

## 問題

Codex Q3: 配布運用の中核 (署名・Updater・SBOM) 未実装。
PH-441 署名 → PH-442 Updater (署名前提で安全設計)。

## 改修

### tauri-plugin-updater 導入

- `src-tauri/Cargo.toml`: `tauri-plugin-updater = "2"` 追加
- `tauri.conf.json` に updater 設定:
  - endpoints: `["https://github.com/emanon-i/arcagate/releases/latest/download/latest.json"]`
  - pubkey: 後で公開鍵生成
- `src-tauri/src/lib.rs` で plugin 初期化

### 署名鍵生成

- `npm run tauri signer generate -- -w ~/.tauri/arcagate.key`
- 公開鍵を tauri.conf.json に embedded
- 秘密鍵は CI secret or local だけ

### フロント UI

- `src/lib/components/settings/UpdaterSettings.svelte` 新設
- 「アップデート確認」ボタン → `check()` IPC
- 利用可能なら download + install + restart 確認 dialog
- Settings > General に統合

### IPC

- `cmd_check_for_updates`: 公開鍵検証付きで latest.json fetch
- `cmd_install_update`: download + 適用

### 受け入れ条件

- [ ] tauri-plugin-updater 依存追加 + Cargo.lock 更新
- [ ] tauri.conf.json に endpoints + pubkey (placeholder OK、署名鍵は別途生成)
- [ ] UpdaterSettings.svelte 新設
- [ ] Settings > General に「アップデート」section
- [ ] cmd_check_for_updates / cmd_install_update IPC
- [ ] README に updater の有効化手順 (秘密鍵管理 / GitHub Releases 設定)
- [ ] `pnpm verify` 全通過

### 別 plan に切り出し

- 実 latest.json サーバ準備 (GitHub Releases 自動投稿 workflow)
- 署名鍵管理の運用文書化
- ロールバック機構
