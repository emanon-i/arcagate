---
id: cross-cutting-item-lifecycle
status: final
type: 横断契約
era: Distribution Hardening
---

# Library Item ライフサイクル契約

> Library item の「いつ追加され、 いつ消され、 何を消すのか」 を網羅した横断契約ドキュメント。 アイテム削除まわりを場当たりに 1 パターンずつ直してきた結果バグが続出 (ウィジェット削除でアイテムが孤立 / ページ削除で手動追加アイテムまで消える 等) した反省から、 全パターンを 1 表に集約し「正しい挙動」 を確定する。

## 用語と所有モデル

### 所有モデル

| 種別                  | 識別                           | 所有者               |
| --------------------- | ------------------------------ | -------------------- |
| **手動 item**         | `source_widget_id IS NULL`     | user (Library 全体)  |
| **監視自動登録 item** | `source_widget_id IS NOT NULL` | 当該監視ウィジェット |

`source_widget_id` / `source_entry_key` 列は migration 039 (PH-CF-100) で追加された back-link。 自動登録経路はこの 2 列を必ず埋める (= 片肺禁止)。

### DB 制約レイヤ (アプリ層 dedupe との関係)

| 対象                                                 | 制約                                                  | 結果                                                                                                                                                                               |
| ---------------------------------------------------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `items.target`                                       | **UNIQUE 無し** (`001_initial.sql`)                   | 同一 target の二重登録は **アプリ層 dedupe (`find_by_target` / `find_by_source`) のみで防止**。 §D2 (bulk delete) / §C9 (import) / CLI / seed 経路では dedupe を経由せず行を作れる |
| `items.source_widget_id` / `source_entry_key`        | 部分 INDEX のみ (`039_items_source_back_link.sql:28`) | UNIQUE 無し → 同 widget の同 entry_key で複数行を作れる (発生しないのは `auto_register_*` 系が `find_by_source` で先に確認するからのみ)                                            |
| `widget_item_hides (widget_id, item_target)`         | PRIMARY KEY (`029_widget_item_hides.sql:19`)          | hide は idempotent (`INSERT OR IGNORE`)、 破綻しない                                                                                                                               |
| `watched_paths.path`                                 | UNIQUE だが **正規化なし** (`003_watched_paths.sql`)  | `C:\x` と `C:/x` は別行として両方登録可能。 現状 `ensureWatchedPath` は UNIQUE 違反を silent skip するため、 区切り混在で重複監視が成立しうる (Codex VERIFY 2026-05-25 §4 指摘)    |
| `workspace_widgets.config` の `item_id` / `item_ids` | TEXT (JSON)、 FK 無し                                 | items への dangling 参照は app 層の `cascade_remove_item_from_widgets` でのみ防止                                                                                                  |

「中途半端な孤立 item」 のほとんどは「app 層 dedupe / cascade を踏まない経路で行を作る or 残す」 ことで発生する。

### 本契約の scope 外

| 経路                                               | 理由                                                                                                                                                                                             |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src-tauri/src/bin/arcagate_cli.rs`                | 開発者向け CLI。 本番アプリの起動経路には含まれない。 ただし backup / 復元用途で勝手に走らせると app 層 dedupe を回避するため、 user 教育で吸収する                                              |
| `src-tauri/examples/seed_dev.rs`                   | 開発 seed。 release ビルドに含めない                                                                                                                                                             |
| `script-folder` widget (`ScriptFolderWatchWidget`) | **items 行を作らない** widget (実体は watched_paths のみ作る `.sh` / `.bat` 監視)。 本契約の生成パターン C1-C9 には登場しない。 ただし widget 削除時の `watched_paths` 解除が漏れている (→ §D19) |

### 参照経路

item を参照する 5 系統:

1. `item_tags` (`sys-ws-<workspace_id>` / `sys-starred` / `sys-type-*` / user tag) — FK CASCADE
2. `workspace_widgets.config` JSON の `item_id` / `item_ids` キー — FK ではない (明示 strip 必要)
3. `widget_item_hides` (`widget_id`, `item_target` = scan entry key) — widget_id FK CASCADE
4. `launch_log` / `item_stats` / `confirmed_items` — FK CASCADE
5. `items.source_widget_id` 自身 (back-link) — `ON DELETE SET NULL` (migration 039)

## 確定済原則 (2026-05-25 user 承認)

1. **手動 item は自動 cascade で消えない**。 explicit user delete (Library 削除 / bulk delete) のみで消える。
2. **監視自動登録 item は所有ウィジェットの削除で削除**。 ただし他 workspace / 他 widget からも参照されているものは残す。
3. **workspace のページ / タブ削除では手動 item を消さない**。 配置 (widget config item_ids) を外すだけ。 消えるのは監視由来の自動登録 item のみ。
4. **Library から監視由来 item を直接削除** → 当該ウィジェットの除外リスト (`widget_item_hides`) に記録、 次の scan で復活させない。 復元は監視ウィジェット設定から (PH-CF-100 / PH-CF-500)。
5. **「画面に見えるのに編集すると壊れる中途半端な孤立 item」 を絶対に生まない**。 すべての削除経路で関連参照 (widget config item_ids / widget_item_hides / launch_log) を後始末する。
6. 上記で決めきれないパターン (再 scan で元ファイル消失 等) は §未確定パターン で選択肢 + 推奨案を提示。

## 生成パターン (9 件)

| #  | パターン名                           | トリガー                             | call-chain (file:line)                                                                                                                                                                  | source 列                                                 | dedupe                                      |
| -- | ------------------------------------ | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------- |
| C1 | manual: ItemFormDialog               | Library「アイテム追加」 button       | `+page.svelte:478-490` → `itemStore.createItem` (`items.svelte.ts:56-60`) → `cmd_create_item` (`item_commands.rs:9-14`) → `item_service.rs:28-79` → `item_repository::insert` (`:9-28`) | NULL / NULL                                               | **無し** (同 target 二重作成可)             |
| C2 | manual: Library D&D                  | OS file drop on Library view         | `+page.svelte:310-314` → ItemFormDialog 経由 (C1 と合流)                                                                                                                                | NULL / NULL                                               | 無し                                        |
| C3 | manual: 詳細 panel「複製」           | LibraryDetailPanel 複製              | `LibraryDetailPanel.svelte:164-177` → `itemStore.createItem` (C1 と合流)                                                                                                                | NULL / NULL                                               | 無し                                        |
| C4 | manual: ブックマーク HTML import     | 設定 → ブックマーク取込              | `BookmarkImport.svelte:82-108` → `itemStore.createItem` (C1 と合流)                                                                                                                     | NULL / NULL                                               | 無し                                        |
| C5 | manual: Undo 復元                    | 削除後 5 秒以内 Undo                 | `library-history.svelte.ts:82-92` → `createItem` IPC (C1 と合流、 新 UUID 生成)                                                                                                         | NULL / NULL                                               | 無し                                        |
| C6 | auto: projects 監視                  | widget mount / `watched_folder` 変更 | `ProjectsWidget.svelte:211-242` → `cmd_auto_register_folder_items` → `item_service.rs:365-480` → insert (`:457`)                                                                        | widget_id / 正規化 entry key                              | `find_by_source` + `widget_item_hides` skip |
| C7 | auto: exe-folder 監視                | scan 完了                            | `ExeFolderWatchWidget.svelte:200-249` → `cmd_register_exe_items_bulk` → `item_service.rs:601-660` (1 transaction) → per-item `register_exe_item_on_conn:505-592` → insert (`:574`)      | widget_id / 第1階層正規化パス                             | `find_by_source` + `widget_item_hides` skip |
| C8 | dead: `cmd_register_exe_item` single | 公開 IPC だが frontend caller 0 件   | `item_service.rs:486-496` → `register_exe_item_on_conn(.., None)`                                                                                                                       | NULL / NULL                                               | `find_by_target`                            |
| C9 | import: JSON                         | 設定 → JSON インポート               | `export_commands.rs:16` → `export_service.rs:91-114` `INSERT OR REPLACE INTO items`                                                                                                     | **column list に source 列含まれず** → 復元で NULL に喪失 | id 一致で REPLACE                           |

### 生成パターンの判定

| #              | 判定 | 理由                                                                                                                                                                                                               |
| -------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| C1, C2, C3, C4 | ✅   | 手動経路、 source NULL で確定原則と整合                                                                                                                                                                            |
| C5 (Undo)      | ⚠️    | snapshot に back-link 列が含まれず、 監視由来 item を Undo すると user-owned に降格 (確定原則 1/2 違反、 「監視所有のまま戻すべき」 か「降格でよい」 か **要確認**)                                                |
| C6, C7         | ✅   | 監視自動登録、 source/entry_key/hide skip すべて確定原則と整合                                                                                                                                                     |
| C8             | ⚠️    | 死路 IPC。 削除推奨 (PH-PQ-500 同型 sweep の対象)                                                                                                                                                                  |
| C9             | ❌   | JSON import の `INSERT OR REPLACE INTO items` 列リストに `source_widget_id` / `source_entry_key` / `card_override_json` が含まれず、 監視由来 item を export → import すると user-owned に降格 (確定原則 1/2 違反) |

## 削除 / 保持 / 孤立トリガー (18 パターン)

凡例:

- **削除**: item DB 行が消える
- **保持**: 行は残る (`source_widget_id` SET NULL / hide 記録 等の状態変化を含む)
- **孤立**: 行は残るが、 参照側に dangling / 整合性破綻

> **2026-05-25 実装後**: 下表「バグ判定 ❌」 の 10 件はすべて `delete_item_with_cleanup` 共通 helper + `is_item_referenced_outside_widget` 判定 + reconcile の `is_enabled=false` 化 + JSON / Undo back-link 復元 等で解消済。 §機械検出 hooks の test / audit が回帰検出する。 詳細は `docs/l3_phases/audit/ITEM_LIFECYCLE_GAP_AUDIT_2026-05-25.md` Bug 1-10。

| #   | パターン名                                           | トリガー                                                                                       | 現状の挙動 (file:line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 正しい挙動 (契約)                                                                                                                                                                                                                                       | バグ判定                                                                                                                             |
| --- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | Library 単独削除                                     | `cmd_delete_item` (Library カード / 詳細 panel / 右クリック)                                   | `item_service.rs:173-197` 1 トランザクション: `find_source_back_link` で監視由来なら `widget_item_hides::add` → `cascade_remove_item_from_widgets` → `item_repository::delete`。 関連参照: `item_tags` / `launch_log` / `item_stats` / `confirmed_items` は FK CASCADE / widget config は明示 strip / `widget_item_hides` は意図的に残す                                                                                                                                                                        | 確定原則 1, 4 と一致                                                                                                                                                                                                                                    | ✅                                                                                                                                   |
| D2  | Library 一括削除 (multi-select)                      | `cmd_bulk_delete_items`                                                                        | `item_service.rs:317-338` 1 トランザクション、 各 id について raw `DELETE FROM items` (`:332`)。 **`find_source_back_link` 呼び出し無し** / **`cascade_remove_item_from_widgets` 呼び出し無し**                                                                                                                                                                                                                                                                                                                 | D1 と同じ後始末を踏むべき (hide 記録 + widget config strip + tx)                                                                                                                                                                                        | ❌                                                                                                                                   |
| D3  | workspace 削除 (`delete_items=true`)                 | `cmd_delete_workspace(id, true)`                                                               | `workspace_service.rs:99-126` 1 トランザクション: `collect_workspace_referenced_item_ids` で 2 経路 (`sys-ws-*` tag ∪ widget config item_ids) 和集合 → `is_item_referenced_outside_workspace` で他 workspace 非参照確認 → `cascade_remove_item_from_widgets` + `DELETE FROM items` (`:113`)。 **`find_source_back_link` 呼び出し無し** → 監視由来 item を消しても hide 記録されず、 widget が残っていれば再 scan で復活                                                                                         | (a) 手動 item を消さない (確定原則 3)、 (b) 監視由来 item を消した場合は hide 記録                                                                                                                                                                      | ❌ 確定原則 3 違反 + hide 記録欠落                                                                                                   |
| D4  | workspace 削除 (`delete_items=false`)                | `cmd_delete_workspace(id, false)`                                                              | `workspace_service.rs:99-126` で items 削除 path を skip、 workspace / widgets / sys-ws-* tag のみ削除                                                                                                                                                                                                                                                                                                                                                                                                          | item は Library に残す。 手動 item は配置情報だけ消える、 監視由来 item は所有 widget が消えるため §D7 (widget 削除) と同じ扱い                                                                                                                         | ⚠️ 確定原則 2 (widget 削除で監視 item を削除) を §D7 で踏襲できているかが連動                                                         |
| D5  | ページ / タブ削除                                    | (現状の UI 上の「タブ」 = `workspaces` row 切替、 タブ単位削除という別経路は **存在しない**)   | grep で `workspace_tab` / `delete_tab` / `tab_id` 0 hit。 タブ削除 = workspace 削除 = D3/D4                                                                                                                                                                                                                                                                                                                                                                                                                     | タブ単位の概念を新設するなら: 手動 item は Library に残し (配置だけ外す)、 監視由来 item は所有 widget が当該タブ配下なら §D7 連鎖                                                                                                                      | ⚠️ 現状仕様で「タブ削除 = workspace 削除」 であることを user 認識と合わせる必要あり (確定原則 3 の文言は「ページ / タブ削除」 を想定) |
| D6  | factory reset (library 部分)                         | `factory_reset(reset_library=true, reset_workspace=false)`                                     | `reset_service.rs:15-47` 1 トランザクション: items / item_tags / tags / launch_log / item_stats / widget_item_hides / icon_cache / openers / themes (custom) を順次 DELETE。 **`cascade_remove_item_from_widgets` 呼び出し無し** → workspace_widgets が残るため widget config の item_ids 全件 dangling                                                                                                                                                                                                         | items 削除前に widget config 経路の strip 必須 (workspace を残すなら)                                                                                                                                                                                   | ❌                                                                                                                                   |
| D7  | 監視ウィジェット削除                                 | `cmd_remove_widget`                                                                            | `workspace_service.rs:180-185` → `workspace_repository.rs:155-161` `DELETE FROM workspace_widgets WHERE id = ?1`。 **items は明示削除されない**。 FK 連動: (a) `items.source_widget_id` `ON DELETE SET NULL` (migration 039) で監視 item は user-owned に降格 (Library 残る) / (b) `widget_item_hides` は CASCADE で消去                                                                                                                                                                                        | 確定原則 2: 監視由来 item を削除 (他 workspace / 他 widget 参照は残す)。 hide 記録も必要 (= widget は消えるが entry を user 削除済として表現するか? = §未確定パターン U-1)                                                                              | ❌ 確定原則 2 違反 — 「降格して残す」 現状 vs 「自動削除」 確定原則                                                                  |
| D8  | watched_path 直接削除                                | `cmd_remove_watched_path` (設定画面)                                                           | `watched_path_service.rs:58-88` **トランザクション無し**: `find_tracked_ids_under_path` (`is_tracked=1` + target prefix match) → 各 id について `cascade_remove_item_from_widgets` + `item_repository::delete` → `watched_paths` 行削除 + `watcher.unwatch`。 **`find_source_back_link` 呼び出し無し**                                                                                                                                                                                                          | 1 トランザクション化 + 監視由来 item については hide 記録                                                                                                                                                                                               | ❌ tx 欠落 + hide 欠落                                                                                                               |
| D9  | 再 scan で元ファイル消失                             | watched_folder 配下のフォルダ / exe が削除・移動 → 次回 widget mount or 設定変更               | `auto_register_folder_items` / `register_exe_items_bulk` 共に scan 結果に出てこない既存 item を検出する経路無し → **DB 行は無傷で残留** (Library 表示可、 launch 失敗)                                                                                                                                                                                                                                                                                                                                          | §未確定パターン U-2: 「自動削除」 vs 「`is_enabled=false` 化」 vs 「現状維持 (launch 時失敗で user 判断)」 を確定する                                                                                                                                   | ❌ (未確定)                                                                                                                          |
| D10 | 監視パス変更                                         | `watched_folder` / `watch_path` config 変更                                                    | widget の `$effect` (`ProjectsWidget.svelte:211-242` / `ExeFolderWatchWidget.svelte:152-250`) が `prevFolder !== folder` 検出して `folderItems = []` + 新 path で再 scan + auto-register。 **旧 path 由来の `source_widget_id == 当 widget` の item は誰も触らない** → 孤立 / **旧 watched_path も解除されない** (cascadeRemoveWatchedPath は widget 削除時のみ)                                                                                                                                                | 旧 entry_key の item を §D7 と同 ruleset で削除、 旧 watched_path を unwatch                                                                                                                                                                            | ❌                                                                                                                                   |
| D11 | EXE 監視: extensions 変更                            | `extensions` config 変更                                                                       | widget の `$effect` (`ExeFolderWatchWidget.svelte:152-250`) で `extensionsKey` 比較 → 新 extensions で再 scan + bulk register。 **旧拡張子で登録された exe item (= 新 scan で出てこない) は残留**。 さらに `find_by_source` が hit すると旧 item の `target`/`label` を新 entry の代わりに使い続ける (`item_service.rs:519-521`)                                                                                                                                                                                | 新 scan 結果に出ない旧 entry を §D7 と同 ruleset で削除                                                                                                                                                                                                 | ❌                                                                                                                                   |
| D12 | EXE 監視: scan_depth 変更                            | `scan_depth` config 変更                                                                       | D11 と同型 (`$effect` で `prevDepth` 比較 → 再 scan)。 深い階層で見つかっていた item が新 depth で対象外になっても残留                                                                                                                                                                                                                                                                                                                                                                                          | D11 と同じ                                                                                                                                                                                                                                              | ❌                                                                                                                                   |
| D13 | ターゲット実体消失 (fs event)                        | watcher が `Remove(_)` イベント検知                                                            | `watcher/mod.rs:101-116` → `watcher_service.rs:41-50` `is_tracked_target` 判定 → `app.emit("item://path-not-found", path_str)` → `+page.svelte:437-441` で toast 表示。 **DB 操作なし**                                                                                                                                                                                                                                                                                                                         | §未確定パターン U-3: 「自動削除」 vs 「`is_enabled=false` 化 + toast」 vs 「現状 toast のみ」 を確定する                                                                                                                                                | ❌ (未確定)                                                                                                                          |
| D14 | ファイル rename / 移動                               | watcher が `Modify(Name(Both))` イベント検知                                                   | `watcher/mod.rs:89-100` → `watcher_service.rs:29-36` `update_target_by_path(old, new)` (`item_repository.rs:189-202` `UPDATE items SET target = ?1 WHERE target = ?2`)。 **`source_entry_key` は更新されない**                                                                                                                                                                                                                                                                                                  | `source_entry_key` も同時更新 (folder rename で 1st-level path が変わる exe-folder ケースで stale → 再 scan で重複 item を作る)                                                                                                                         | ❌                                                                                                                                   |
| D15 | fs Create イベント (新規 dir / file)                 | watcher が `Create(_)` イベント検知                                                            | `watcher/mod.rs:77-88` で `app.emit("folder://new-directory", path_str)`。 frontend に listener なし (PH-CF-500 D2 で `auto_add` 撤廃)。 **完全に死路**                                                                                                                                                                                                                                                                                                                                                         | §未確定パターン U-4: イベント emit を撤廃 (backend 計算節約) vs 何らかの reconcile に活用                                                                                                                                                               | ⚠️ (dead path だが害なし、 撤廃推奨)                                                                                                  |
| D16 | 複数参照のうち 1 参照だけ解除                        | 当該 item が複数の widget config から参照されている状態で、 1 widget のみ削除 / 1 配置のみ外す | (a) widget 削除: `workspace_widgets` 行が消えれば config 経路は当該行と共に消える、 item 自体は別 widget config / 別 workspace tag から参照され続けるなら残る (`is_item_referenced_outside_workspace` `workspace_repository.rs:308-337`) / (b) 1 配置だけ外す: 未実装 (§D17)                                                                                                                                                                                                                                    | 確定原則: 他参照がある限り残す                                                                                                                                                                                                                          | ✅ (現状で正しく動作)                                                                                                                |
| D17 | アイテムを workspace から外す (Library 残し)         | (現状の UI に「ワークスペースから外す」 専用操作は **存在しない**)                             | grep で「remove from workspace」 「外す」 関連の専用 IPC 無し。 widget 右クリック「この widget から外す」 (`WidgetItemContextMenu.svelte:104` `handleHideFromWidget` → `widgetItemHidesStore.add`) は **per-widget hide であって Library から外す操作ではない**                                                                                                                                                                                                                                                 | §未確定パターン U-5: 専用 operation を新設する (「Library 残し / 当該 workspace から item_ids 配列除去」)、 削除 (D1) と区別                                                                                                                            | ❌ (未実装、 確定原則 3 の補完として要設計)                                                                                          |
| D18 | kill_switch                                          | `kill_switch_service.rs` の起動時 disabled.json 取得                                           | DB 操作無し (バージョン阻止のみ)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | items に触らない                                                                                                                                                                                                                                        | ✅                                                                                                                                   |
| D19 | script-folder widget 削除時の watched_paths 解除漏れ | 通常の `cmd_remove_widget` (script-folder widget を active workspace から削除)                 | `workspace-widgets.svelte.ts:46-55` の `extractWatchedPathFromWidget` が **exe_folder / projects のみ対応** で script-folder の `watch_path` を返さない → `cascadeRemoveWatchedPath` (`:65-83`) で同 path が拾われず `watched_paths` 行が永続残留 (= 監視リーク)。 items 自体を作らない widget なので孤立 item は出ないが、 watcher は回り続け FS リソースを浪費する                                                                                                                                            | exe_folder / projects と同型に script-folder の `watch_path` を抽出して同 cascade に乗せる                                                                                                                                                              | ❌ (Codex VERIFY 2026-05-25 §2-1)                                                                                                    |
| D20 | cross-workspace で同 path 共有時の早期 unwatch       | 監視 widget (exe/projects) を 1 workspace から削除                                             | `workspace-widgets.svelte.ts:71` `stillUsed = remaining.some(...)` が **active workspace の widgets 配列のみ**を見る。 他 workspace に同 `watch_path` の widget があっても見えず `watched_paths` を unwatch してしまう。 「他 workspace に切替で `ensureWatchedPath` が UNIQUE skip で再登録」 self-heal 前提だが、 unwatch と同時に `remove_watched_path` が呼ばれ backend `find_tracked_ids_under_path` (`watched_path_service.rs:66-71`) で **共有 path 下の tracked item を全部 cascade DELETE** してしまう | watched_paths の参照判定を全 workspace の widgets 横断で行う (or backend に `is_path_referenced_by_any_widget(path)` を新設) / fallback として `find_tracked_ids_under_path` cascade を「実際 watcher 解除前に全 workspace 横断 referencer 0 を再確認」 | ❌ (Codex VERIFY 2026-05-25 §2-2)                                                                                                    |

## 関連参照のクリーンアップ summary

| 参照                                              | 経路                                                  | 後始末方法                                       | 健全性                                                                                 |
| ------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------- |
| `item_tags`                                       | 全経路                                                | FK CASCADE (`001:58`)                            | ✅                                                                                     |
| `launch_log`                                      | 全経路                                                | FK CASCADE (`001:66`)                            | ✅                                                                                     |
| `item_stats`                                      | 全経路                                                | FK CASCADE (`001:76`)                            | ✅                                                                                     |
| `confirmed_items`                                 | 全経路                                                | FK CASCADE (`033:15`)                            | ✅                                                                                     |
| `workspace_widgets.config` `item_id` / `item_ids` | D1, D3, D8 のみ                                       | 明示 `cascade_remove_item_from_widgets` で strip | ❌ D2 (bulk) / D6 (reset library のみ) で漏れ                                          |
| `widget_item_hides`                               | D1 が **追加** (復活抑制契約)、 widget 削除で CASCADE | item 削除では触らず残す設計                      | ⚠️ D2 / D3 / D6 / D8 で `find_source_back_link` → `widget_item_hides::add` が漏れている |
| `items.source_widget_id` (back-link)              | widget 削除時                                         | FK `ON DELETE SET NULL` で降格                   | ⚠️ 確定原則 2 (自動削除) と矛盾                                                         |

## 確定済 9 件 (旧 §未確定パターン、 2026-05-25 user 確定)

旧「未確定パターン」 を user 判断で確定し、 実装した。 詳細実装は本ブランチで完遂。

| ID   | パターン                                                   | 確定方針                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 実装場所                                                                                                                                                                                                            |
| ---- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U-1  | 監視 widget 削除時の自動 item 扱い                         | **(a) 削除** (確定原則 2)。 他参照 (他 workspace / 他 widget config) があれば残し、 `source_widget_id` は FK `ON DELETE SET NULL` で降格                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `workspace_service::remove_widget` で `find_owned_items_by_widget` → `is_item_referenced_outside_widget` 判定 → `delete_item_with_cleanup`                                                                          |
| U-2  | 再 scan で元ファイル消失                                   | **(b) `is_enabled=false` でグレーアウト** (削除しない、 launch で気づける + grey-out で目視で気づける)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `auto_register_folder_items` / `register_exe_items_bulk` 内で `find_stale_owned_entries` → `set_is_enabled(false)`、 再 scan で再発見されたら `set_is_enabled(true)` で復活                                         |
| U-3  | ターゲット実体消失 (fs Remove)                             | **(b) `is_enabled=false` + toast** (path-not-found event に item_id を含めて frontend 通知)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `watcher_service::disable_tracked_target` を `watcher/mod.rs` の Remove handler から呼ぶ。 frontend `+page.svelte` の `item://path-not-found` listener で itemStore refresh + info toast                            |
| U-4  | fs Create イベント                                         | **(a) emit 撤廃** (dead path、 PH-CF-500 D2 で auto_add 撤廃済)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `watcher/mod.rs` の `Create(_)` handler は debug log のみ                                                                                                                                                           |
| U-5  | 「workspace から外す」 専用 operation                      | **(a) 新設**。 `cmd_remove_item_from_workspace(workspace_id, item_id)` で sys-ws-* tag 解除 + 当該 workspace の widget config item 参照 strip、 item 行は残す                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `item_service::remove_item_from_workspace` + `workspace_repository::cascade_remove_item_from_workspace_widgets` + `WidgetItemContextMenu.svelte` の新規 menu item「このワークスペースから外す」                     |
| U-6  | Undo の back-link 復元                                     | **(a) 完全復元**。 `CreateItemInput` に `source_widget_id` / `source_entry_key` を追加し、 backend で源 widget の存在チェック → 不在なら NULL フォールバック (FK 安全)                                                                                                                                                                                                                                                                                                                                                                                                                                        | `models/item.rs` `CreateItemInput` 拡張 + `item_service::create_item` の FK 安全チェック + `library-history.svelte.ts` `undo()` が snapshot から渡す                                                                |
| U-7  | JSON export / import の back-link                          | **(a) 完全復元**。 import 側 SQL の column list に source 列追加 + 源 widget 不在時 NULL フォールバック                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `export_service::import_json` SQL 拡張 + safe_source_widget_id 判定                                                                                                                                                 |
| U-8  | 設定変更 (path/extensions/depth) の reconcile              | **(b) backend reconcile**。 `auto_register_*` 系は scan 全結果を 1 transaction で受け取り、 stale entry (`source_widget_id == widget_id` AND `source_entry_key ∉ new_keys`) を `is_enabled=false` 化                                                                                                                                                                                                                                                                                                                                                                                                          | U-2 と同経路で `find_stale_owned_entries` → `set_is_enabled`                                                                                                                                                        |
| U-9  | タブ削除 semantics                                         | **(a) タブ = workspace 削除** で doc 統一。 確定原則 3 の文言は「workspace 削除では手動 item を消さない」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | doc 反映 (本ファイル)。 タブ単位の独立 operation は新設しない                                                                                                                                                       |
| U-10 | 監視 widget の起動 EXE 切替時の target 同期 (PH-CF-1200 ⑧) | **(a) backend で同期**。 `register_exe_item_on_conn` が source 経由で既存 item を再発見した時、 scan が渡してきた新 path (= override 反映後) と既存 item.target が異なれば、 `item_repository::set_source_target` で `target` (+ 派生 `label`) を **その transaction 内で** 書き戻す。 user 編集列 (`args` / `default_app` / `card_override_json` / `aliases` / `is_tracked` / `icon_path`) は touch しない契約。 旧実装は既存 item をそのまま return しており、 ExeFolder widget で起動 EXE を切り替えても Library item の target が古い path のまま残留 → Library 経路の起動で "not found" を起こしていた。 | `item_service::register_exe_item_on_conn` + `item_repository::set_source_target`、 test: `test_register_exe_items_bulk_overrides_existing_target` / `test_register_exe_items_bulk_target_sync_preserves_user_edits` |

## 機械検出 hooks

| 確定原則                                   | 機械検出方法                                                                                                                                                                                                                                                                                                    | 既存 / 新規 |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 1 / 3: 手動 item は自動 cascade で消えない | Rust 統合 test: `test_delete_workspace_deletes_only_monitored_items` / `test_delete_workspace_keeps_manual_items_in_widget_config` / `test_delete_workspace_mixed_payload_no_orphans_or_dangling`                                                                                                               |             |
| 2: 監視 item は所有 widget 削除で削除      | `workspace_service::remove_widget` primary path (owned items 削除 + hide 記録、 `find_owned_items_by_widget` + `is_item_referenced_outside_widget`) + FK `ON DELETE SET NULL` defensive fallback (既存 `test_widget_delete_cascades_hides_and_sets_null_source`)                                                |             |
| 4: 監視由来直接削除で復活抑制              | 既存 `test_projects_auto_register_delete_no_resurrection` + 新規 `test_bulk_delete_records_hide_for_monitored_items` (bulk 経路でも復活抑制契約成立)                                                                                                                                                            |             |
| 5: 孤立 / ghost 不在                       | (a) `scripts/audit-source-back-link.sh` 片肺 audit + (b) **`scripts/audit-item-cascade-helper.sh` 新設** (`DELETE FROM items` の caller は `delete_item_with_cleanup` 経由を強制、 allowlist は repository::delete と reset_service の bulk 経路のみ) + (c) 統合 test (mixed_payload で widget config 残留検査) |             |
| U-2 / U-8: 再 scan reconcile               | `test_auto_register_disables_stale_items` (stale → is_enabled=false) + `test_auto_register_re_enables_revived_items` (再発見 → 復活)                                                                                                                                                                            |             |
| U-5: workspace 配置解除                    | `test_remove_item_from_workspace_keeps_item_strips_refs`                                                                                                                                                                                                                                                        |             |
| U-10 (PH-CF-1200 ⑧): override target 同期  | `test_register_exe_items_bulk_overrides_existing_target` (新 path で target が更新される) + `test_register_exe_items_bulk_target_sync_preserves_user_edits` (args / default_app / aliases / card_override_json 等 user 編集列が温存される)                                                                      | 新規        |

## 共通 helper による集約 (実装済)

`delete_item_with_cleanup(conn, item_id)` 共通 helper を `item_service.rs` に新設し、 D1 / D2 / D3 / D6 / D7 / D8 の全経路から呼ぶ:

- helper の責務: (1) `find_source_back_link` で監視由来チェック → `widget_item_hides::add` で hide 記録、 (2) `cascade_remove_item_from_widgets`、 (3) `item_repository::delete`
- 「いつ item を消すか」 を経路ごとに分岐させ、 「**何を消すか**」 は helper 1 箇所に集約する設計
- `scripts/audit-item-cascade-helper.sh` で「`DELETE FROM items` を実行する経路は helper を経由する」 を機械検出 (allowlist: `item_repository::delete` (= helper の primitive) と `reset_service::factory_reset` (= 全件 reset))

経路ごとの利用:

| 経路                                     | 削除単位                                   | helper 経由                            | tx                                 |
| ---------------------------------------- | ------------------------------------------ | -------------------------------------- | ---------------------------------- |
| D1 `delete_item`                         | 1 件                                       | ✅ 直接呼出                            | 1 tx                               |
| D2 `bulk_delete_items`                   | N 件 (≤1000)                               | ✅ loop                                | 1 tx                               |
| D3 `delete_workspace(delete_items=true)` | 監視 owned のみ                            | ✅ loop                                | 1 tx                               |
| D6 `factory_reset(reset_library=true)`   | 全件 (widget config strip 後に raw DELETE) | 部分 (cascade のみ helper でなく repo) | 1 tx                               |
| D7 `remove_widget`                       | owned items のうち他参照無いもの           | ✅ loop                                | 1 tx                               |
| D8 `remove_watched_path`                 | tracked under path                         | ✅ loop                                | 1 tx (Bug 5 修正で transaction 化) |

## 参照

- `docs/l3_phases/audit/CODEX_ITEM_LIFECYCLE_2026-05-25.md` — Codex 独立列挙結果 (1 回目、 生成 / 削除 / 保持 / 設定 / FS の 5 軸 + POTENTIAL BUG LIST)
- `docs/l3_phases/audit/CODEX_ITEM_LIFECYCLE_VERIFY_2026-05-25.md` — Codex 漏れ検証 (2 回目、 script-folder cascade 漏れ / cross-workspace 早期 unwatch / UNIQUE 制約 / CLI 経路 を追加発見)
- `docs/l3_phases/audit/ITEM_LIFECYCLE_GAP_AUDIT_2026-05-25.md` — gap 分析詳細 (本契約の根拠データ、 全パターン総数 / バグ判定 / 要確認 を network 図で整理)
- `docs/l3_phases/clean-feedback/PH-CF-100_workspace-library-integrity.md` — back-link + reverse lifecycle data model
- `docs/l3_phases/clean-feedback/PH-CF-500_watch-widget-chrome.md` — 除外 / 復元 UI
- migration: `001_initial.sql` (FK CASCADE) / `003_watched_paths.sql` / `029_widget_item_hides.sql` / `033_confirmed_items.sql` / `039_items_source_back_link.sql`
- 主要 service: `src-tauri/src/services/item_service.rs` / `workspace_service.rs` / `watched_path_service.rs` / `reset_service.rs` / `watcher_service.rs` / `watcher/mod.rs`
